project_id = "yltbknkksjgtexluhtau"

# Allow 360Messenger to GET the PDF without Authorization header (proxy still validates access_key)
[functions.whatsapp-pdf-proxy]
verify_jwt = false

# Backup scheduler: no JWT; auth via BACKUP_CRON_SECRET (Authorization: Bearer <secret>)
[functions.backup-scheduler]
verify_jwt = false