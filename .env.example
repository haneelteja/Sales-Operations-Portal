# Supabase Configuration
# Copy this file to .env and fill in your actual values
# Never commit .env to version control

VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional: Enable mock authentication in development
# VITE_USE_MOCK_AUTH=false

# Optional: Use Resend email service
# VITE_USE_RESEND_EMAIL=false

# Optional: App URL for password reset links (must include /verify in Supabase Redirect URLs)
# Add https://your-domain.com/verify to Supabase Dashboard > Auth > URL Configuration > Redirect URLs
# VITE_APP_URL=http://localhost:8080
